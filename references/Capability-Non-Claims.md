# Capability Non-Claims

Do not assume a specific Google Flow SDK method or a separate negative-prompt parameter exists. The Tool Maker must inspect the current runtime/code/types before mapping FinalPrompt into generation. Unsupported capabilities remain semantic text or block generation when they are hard requirements.
