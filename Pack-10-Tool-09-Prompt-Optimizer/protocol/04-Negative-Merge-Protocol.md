# 04 — Negative Merge Protocol

If runtime has verified native negative-prompt support, map accordingly.

If not:
append `[NEGATIVE]` block to final prompt text.

Never invent an unsupported parameter.
