# Example 06 — Negative Field Fallback

Runtime capability does not verify a separate negativePrompt parameter.

Correct:
include `[NEGATIVE]` block in final prompt text.

Incorrect:
invent `negativePrompt:` API option.
