# 03 — Minimal Delta Repair

If QA says only neckline/hand behavior failed:
change Motion/Negative constraints around hand-neckline interaction.

Do not rewrite:
- ratio if passed;
- camera path if passed;
- background if passed;
- cinematic grade if passed.

This is the core anti-regression strategy.
