# 17 — Known Limits

A compact prompt cannot guarantee model compliance. Compression may require tradeoffs among soft instructions. Runtime capabilities and prompt parsing can change. Hard locks remain mandatory; QA must review every new generation.
